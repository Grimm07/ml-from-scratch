```
Linear/Logistic Regression
└── sklearn.datasets (California Housing, Iris, Wine)

K-Means, PCA
└── sklearn.datasets (Iris, digits, make_blobs)

Neural Networks (MLP)
└── sklearn.datasets.fetch_openml('mnist_784')
└── torchvision.datasets.FashionMNIST

CNN
└── torchvision.datasets.MNIST
└── torchvision.datasets.FashionMNIST
└── torchvision.datasets.CIFAR10 (if you want harder)

Transformer (text)
└── Hugging Face datasets (imdb, wikitext-2)
└── Or simple text file (Shakespeare, stored in repo)

GAN
└── torchvision.datasets.FashionMNIST
└── CelebA (if going advanced)

Gradient Boosting
└── sklearn.datasets (California Housing, Wine)
└── Kaggle: Titanic (optional, for feature engineering showcase)
```
