"""Post-order traversal: left subtree, right subtree, then node.

Useful for operations that must visit children before the parent
(e.g. computing subtree sizes, freeing nodes).
"""


def postorder():
    raise NotImplementedError()
